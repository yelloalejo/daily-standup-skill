{
  "allowedMcpPatterns": [
    { "pattern": "list", "comment": "All list operations (issues, projects, cycles)" },
    { "pattern": "get", "comment": "All get/read operations" },
    { "pattern": "search", "comment": "All search operations" }
  ]
}
